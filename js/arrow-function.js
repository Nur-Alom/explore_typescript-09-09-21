"use strict";
// Normal Function Type.
function add(num1, num2) {
    return num1 + num2;
}
// single Line Arrow Function Type.
const add2 = (num1, num2) => num1 + num2;
// Multiline Arrow Function Type.
const doMath = (x, y) => {
};
